@using Microsoft.AspNetCore.Identity
@using AgendaApp.Controllers
@inject SignInManager<Gebruiker> SignInManager
@inject UserManager<Gebruiker> UserManager
@inject HomeController home 

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        @*<li class="nav-item">
                <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hallo @User.Identity.Name!</a>
            </li>*@
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Instructions", "Home", new { area = "" })">
                <button id="" type="submit" class="nav-link btn btn-link text-light uitlogknop" onclick="@home.EndTimer("Logout", User.Identity.Name)">Uitloggen</button>
            </form>
        </li>
    }
</ul>


